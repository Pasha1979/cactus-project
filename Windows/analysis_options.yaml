include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - ../REFACTORING_ROADMAP/Стабильная/**
  strong-mode:
    implicit-casts: false
    implicit-dynamic: false

linter:
  rules:
    avoid_print: false