// test/widget_test.dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Dummy test', (WidgetTester tester) async {
    // Этот тест ничего не делает — просто чтобы не было предупреждений
    expect(true, isTrue);
  });
}
